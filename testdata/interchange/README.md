# Interchange fixtures

Canonical RAD Activity Package fixtures are specified by `RADM-INT_R00.md`.
Their serializers, validators, and round-trip cases are deferred until the
interchange implementation milestone is deliberately introduced; M1 does not
implement the deferred R00 transfer workflow.
