/**
 * Android-independent RADM semantics.
 *
 * Types in this package deliberately use computational units rather than
 * presentation strings. Android, Room, Compose, MapLibre, and Vico types are
 * translated at package boundaries and are not domain contracts.
 */
package com.jeppe.radm.domain
